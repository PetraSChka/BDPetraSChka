CREATE TABLE TEAMS (
	TEAM_ID NUMBER GENERATED ALWAYS AS IDENTITY INCREMENT BY 1 START WITH 1 MINVALUE 1 NOT NULL,
	LEAGUE_ID NUMBER NOT NULL,
	NAME VARCHAR2(100),
	YEAR_OF_FOUNDATION NUMBER,
	CONSTRAINTS TEAM_ID_PK PRIMARY KEY (TEAM_ID)
);